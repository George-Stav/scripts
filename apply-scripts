#!/bin/sh

symlinks="$HOME/.local/bin"
find "$HOME/repos/scripts/scripts/" -type f | while read -r s; do
	[ ! -x "$s" ] && chmod +x "$s"
	ln -sf "$s" "$symlinks"
done

# remove broken symlinks
find -L "$symlinks" \
	-type l \
	-exec rm {} \;
